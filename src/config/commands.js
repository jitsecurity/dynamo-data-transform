const COMMAND_DESCRIPTION = {
  up: 'Run the pending data transformation scripts',
  down: 'Rollback the last data transformation',
  history: 'Show the history of data transformations',
  prepare: 'Prepare data for the data transformation script',
  init: 'Initialize the data-transformations folder',
};

module.exports = {
  COMMAND_DESCRIPTION,
};
